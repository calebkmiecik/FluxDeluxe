# Dependencies for the embedded Python (backend subprocesses).
# These are installed into the bundled python/ directory by build.py.
# Includes everything DynamoPy, Streamlit tools, and shared libs need.
#
# Pinned to match the Dynamo3.11 conda env where possible.

# --- Networking / Backend ---
python-socketio==5.12.1
python-engineio==4.11.2
msgpack==1.1.0
requests==2.32.3
websocket-client==1.8.0
Flask==3.1.0
uvicorn==0.34.0
simple-websocket==1.1.0

# --- Data ---
numpy==1.26.0
pandas==2.2.3
pyarrow>=23.0.0
scipy>=1.16.0

# --- Firebase / Google Cloud ---
firebase-admin==6.6.0
google-cloud-firestore==2.20.1
google-cloud-storage==3.1.0
google-api-python-client==2.162.0
google-auth==2.38.0
grpcio==1.71.0rc2
grpcio-status==1.71.0rc2
protobuf==5.29.3

# --- Streamlit (Metrics Editor, AxioDash) ---
streamlit==1.53.1
altair>=6.0.0
# pydeck removed — not used, saves ~15 MB

# --- Document processing (Metrics Editor) ---
python-docx==1.2.0
lxml>=6.0.0

# --- Serial / Hardware (FluxLite backend) ---
pyserial==3.5
pyserial-asyncio==0.6

# --- ML (tflite-runtime only, NOT full TensorFlow) ---
# tflite-runtime is installed separately from a local wheel in DynamoPy.
# See build.py step 2 for the wheel install.
numba>=0.62.0

# --- Misc ---
tzlocal==5.3.1
psutil>=7.0.0
PyYAML>=6.0.0
concurrent-log-handler>=0.9.28
GitPython>=3.1.46
Pint>=0.24.0
