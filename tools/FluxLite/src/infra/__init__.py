"""Infrastructure helpers (pure, non-Qt).

Keep low-level utilities (HTTP, address resolution, parsing helpers) here so they
can be reused by app-services and UI without creating circular dependencies.
"""


